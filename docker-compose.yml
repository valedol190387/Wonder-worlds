services:
  wonderworlds:
    build: .
    restart: unless-stopped
    ports:
      - "8123:8123"
    environment:
      - PORT=8123
      - DATA_DIR=/data
    volumes:
      - wonderworlds-data:/data

volumes:
  wonderworlds-data:
